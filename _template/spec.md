# 功能规格：<需求名称>

**功能目录**：`<编号-需求名>`  
**创建日期**：`<YYYY-MM-DD>`  
**状态**：Draft  
**输入**：`<粘贴或概括用户原始需求，保留关键路径、字段名、日志、时间、异常口径>`

## 背景

- 当前问题：`<说明为什么要改>`
- 当前行为：`<说明现有代码行为>`
- 目标行为：`<说明改完后应如何表现>`
- 非目标：`<说明本次明确不做什么>`

## 用户场景与测试 *(必填)*

### 用户故事 1 - <场景名称>（优先级：P1）

`<用业务语言描述用户期望。>`

**独立测试**：`<说明如何单独验证这个故事。>`

**验收场景**：

1. **Given** `<前置条件>`，**When** `<动作>`，**Then** `<结果>`。
2. **Given** `<边界条件>`，**When** `<动作>`，**Then** `<结果>`。

### 用户故事 2 - <场景名称>（优先级：P2）

`<按需保留或删除。>`

**独立测试**：`<说明如何单独验证。>`

**验收场景**：

1. **Given** `<前置条件>`，**When** `<动作>`，**Then** `<结果>`。

## 历史问题防漏分析 *(强制)*

- 关键参数来源和赋值时机：
  - `<参数名>`：来源 `<类/方法/字段>`；赋值时机 `<调用前/当前层现算/外部返回>`；下游读取位置 `<类/方法>`。
- 下游读取字段清单：
  - `<方法名>` 读取 `<字段 1>`、`<字段 2>`、`<字段 3>`。
- 空对象 / 占位对象风险：
  - 是否存在 `new XxxDto()`、空 JSON、空 Map 或只 set 部分字段后继续传递：`<是/否；如是，写处理策略>`。
- 调用顺序风险：
  - 是否存在调用后才赋值、异步后才赋值、或依赖后续流程补齐字段：`<是/否；如是，写处理策略>`。
- 旧逻辑保持：
  - `<列出必须不变的过滤、日志、延迟、异常、幂等、fallback、权限、标签、MQ 字段等行为>`。
- 需要用户确认的设计选择：
  - `<列出会改变业务语义、引入额外远程调用、修改接口契约或扩大影响范围的选择；没有则写“无”>`。

## 边界情况

- `<空值、缺字段、非法枚举、旧消息兼容、配置缺失、远程调用失败、重复触发、并发、时间窗口等>`
- `<明确哪些边界跳过、哪些兜底、哪些抛错或记录日志>`

## 需求 *(必填)*

### 功能需求

- **FR-001**：系统 MUST `<明确可实现需求>`。
- **FR-002**：系统 MUST `<明确可实现需求>`。
- **FR-003**：系统 MUST NOT `<明确禁止改变的行为>`。
- **FR-004**：单元测试 MUST `<明确测试要求>`。

## 成功标准 *(必填)*

- **SC-001**：`<可衡量结果>`。
- **SC-002**：`<可衡量结果>`。
- **SC-003**：`<不回归标准>`。

## 假设

- `<无法从代码确认但本规格默认采用的口径>`。
- `<如假设被推翻，需要追加 Dxxx 纠正记录>`。

## 执行记录

### D001 - 文档记录

- 已创建本 Spec Kit 文档。
- 已完成历史问题防漏分析和强制门禁检查。
- 本阶段未修改业务代码。

### D002 - 实现记录

- `<实现后填写：实现内容、影响范围、测试命令、测试结果、自检结论。>`

### D003 - 纠正记录模板

- 触发原因：`<用户补充/测试失败/代码审查发现/参数遗漏/调用顺序问题>`。
- 修正内容：`<写清楚旧口径和新口径>`。
- 文档同步：`<spec/tasks/AGENTS/checklist 是否已同步>`。
- 验证结果：`<测试或静态检查结果>`。
