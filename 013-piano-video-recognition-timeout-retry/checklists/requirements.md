# 规格质量检查清单：钢琴视频识别超时后单次延迟重试

**用途**：验证钢琴视频识别超时延迟重试需求完整性和实现可测性  
**创建日期**：2026-05-11  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标代码文件。
- [x] 明确目标方法为 `PianoVideoHomeWorkHandleServiceImpl#handle`。
- [x] 明确首次等待窗口为 10 分钟。
- [x] 明确首次超时后延迟时间为 7 分钟。
- [x] 明确延迟后再次调用 `triggerAsyncRecognitionIfNeeded`。
- [x] 明确重试后再次等待 10 分钟。
- [x] 明确重试后不再继续重试。
- [x] 明确本次只先创建文档，不编码。
- [x] 所有必填章节已完成。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 标记残留。
- [x] 需求可测试且无歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖首次超时、7 分钟延迟、重试触发、第二次等待和不再重试。
- [x] 边界情况已识别。

## 实施就绪度

- [x] 实现范围限定在 `fc/sop-reply` 模块。
- [x] 不涉及数据库表结构、OTS 表结构、Redis key 结构或配置中心接口调整。
- [x] 明确重试前需要重新读取缓存以避免重复触发。
- [x] 明确需要编译检查。
- [x] `PianoVideoHomeWorkHandleServiceImpl.java` 已按规格实现。
- [x] `fc/sop-reply` 模块已编译验证通过。

## 备注

- 已完成业务代码实现。
- 等待逻辑已能区分成功、失败和超时，`handle` 仅在首次超时后进入 7 分钟延迟重试。
