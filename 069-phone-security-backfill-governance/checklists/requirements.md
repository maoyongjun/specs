# 需求检查清单：手机号安全字段上线与回填治理

## 范围

- [x] 已创建独立 spec-kit 目录。
- [x] 已记录最终 DDL 和索引 SQL 文件。
- [x] 已记录 `drh_specail_user` 废弃并排除。
- [x] 已记录 P3 四张表纳入回填。
- [x] 已记录后续新增表维护步骤。

## DDL

- [x] 字段 DDL 和索引 DDL 已分开。
- [x] `phone_md5` / `receiver_phone_md5` / `reciver_phone_md5` 均有索引。
- [x] `phone_aes` / `receiver_phone_aes` / `reciver_phone_aes` 均有索引。
- [x] 未包含废弃表 `drh_specail_user`。

## 回填接口

- [x] 加密字段回填和明文掩码覆盖共用同一目标清单。
- [x] 加密字段回填不覆盖原明文字段。
- [x] 明文掩码覆盖不调用 FC。
- [x] 明文掩码覆盖不修改 `md5/aes`。
- [x] 明文掩码覆盖支持 `dryRun`。
- [x] 明文掩码覆盖与加密字段回填运行态互斥。

## 风险

- [x] 已记录执行顺序硬约束。
- [x] 已记录明文掩码覆盖后不得重跑加密字段回填。
- [x] 已记录 `aes` 为空时不覆盖明文字段。
- [x] 已记录生产首次执行必须先 dry-run。

## 验证

- [x] 单测不连接真实 DB。
- [x] 单测不调用 FC。
- [x] 单测覆盖 SQL 门禁和列拷贝。
- [x] 相关单测已在本机跑通。
- [ ] 测试库已执行 dry-run 验证。
