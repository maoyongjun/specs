# 规格质量检查清单：P1 接口安全整改清单影响表整理

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-10`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增、修改和禁止改变的行为。
- [x] 明确后续实现必须增加测试或静态验证记录。
- [x] 明确本规格不修改业务代码、不新增 DDL、不执行回填。

## 需求完整性

- [x] 无待澄清标记或未替换占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量。
- [x] 接口矩阵覆盖 CSV 13 条非空整改记录。
- [x] 边界情况已识别，并明确跳过、兜底、返回空或记录风险的策略。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机。
- [x] 已列出下游读取字段清单。
- [x] 已记录 `setEntity(appletUserDo)` 和创建链路只 set `phone` 的占位风险。
- [x] 已明确手机号查询必须在构造 Wrapper / Mapper 参数前现算 `phoneMd5`。
- [x] 已记录不允许先按明文字段查询再返回阶段掩码。
- [x] 已记录导出、响应展示、保存链路和非 HTTP 任务的验证方案。
- [x] 模糊搜索、外呼 / 短信明文使用边界已记录为需后续确认。

## 实施就绪度

- [x] 实现范围已限定为新增规格目录，不扩散到无关模块。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ/Redis/配置契约。
- [x] 已确认旧逻辑中必须保持不变的接口路径、分页参数、权限过滤、导出结构和外部供应商必要明文使用。
- [x] 每个关键需求至少有一条静态验证或后续接口验证任务。
- [x] 单元测试计划未要求真实访问 Redis、OTS、Center、RocketMQ、FC 或外部 HTTP。
- [x] 已同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`。

## 备注

- 本规格是影响表整理文档，不代表代码整改已完成。
- 后续进入代码修复时，应按矩阵逐项补充编译、单测或接口验证记录。
