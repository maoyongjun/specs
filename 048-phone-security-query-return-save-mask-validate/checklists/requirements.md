# 规格质量检查清单：手机号安全字段查询返回与掩码入参校验

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-03`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目（drh、ju-chat）、模块（drh-common、drh-kk-cms、drh-pay、drh-endpoint、drh-callback、drh-media-process、ai-common、ai）和核心实现位置。
- [x] 明确用户目标（查询返回安全字段、phone 返回掩码、保存拒绝掩码格式）、成功标准和非目标。
- [x] 明确新增（Output/DTO 增加安全字段、phone 返回掩码值、掩码格式校验）、修改（phone 赋值来源）和禁止改变的行为（查询逻辑、保存逻辑、MD5 拒绝逻辑）。
- [x] 明确兼容性（phone_mask 为 NULL 时的 fallback）、异常处理（安全字段为 NULL 不抛异常）和错误提示（掩码格式提示 vs MD5 格式提示）。
- [x] 明确后续实现必须增加测试或静态验证记录。

## 需求完整性

- [x] 无待澄清标记或未替换占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量（7 条成功标准）。
- [x] 验收场景覆盖正常路径（明文/AES 保存）、边界路径（NULL 安全字段、掩码入参）和不回归路径（MD5 拒绝不变、后端间接口不受影响）。
- [x] 边界情况已识别（10+ 条），并明确跳过、兜底、抛错或记录日志的策略。

## 参数完整性门禁

- [x] 已列出关键参数来源（`phoneMask`、`phoneMd5`、`phoneAes` 来自数据库字段通过实体 getter 获取）。
- [x] 已列出赋值时机（查询结果映射到 Output/DTO 时）。
- [x] 已识别自动映射（BeanUtils.copyProperties / ConvertUtil）场景的 `phone` 字段覆盖风险。
- [x] 已要求安全字段在查询结果映射时同步赋值。
- [x] 已要求掩码格式校验在 `isWritablePhoneInput()` 之前执行。
- [x] 已明确保存/更新入口在 `createAesInfo()` 之前执行掩码格式校验。
- [x] 已记录业务语义差异：掩码格式错误提示与 MD5 格式错误提示不同。
- [x] 已为 drh 和 ju-chat 两个工程分别列出涉及的 Output/DTO 和 Service 文件。

## 实施就绪度

- [x] 实现范围已限定（只改 Output/DTO 字段增加、phone 赋值来源改为掩码、保存入口增加掩码格式校验）。
- [x] 不新增数据库表、字段或索引（安全字段已由 032 添加）。
- [x] 不新增对外 API（只修改现有接口的返回结构和校验逻辑）。
- [x] 不修改 MQ/Redis/配置契约。
- [x] 已确认旧逻辑中必须保持不变的查询、保存、MD5 拒绝和后端间调用。
- [x] 每个关键需求至少有一条测试或静态验证任务。
- [x] 单元测试计划避免真实访问 Redis、OTS、Center、RocketMQ、FC 或外部 HTTP。
- [x] 前置需求（032/036/041）关系已明确。

## 专项检查：查询返回安全字段

- [x] 已明确 drh 工程需修改的 Output/DTO 类清单（11+ 个类）。
- [x] 已明确 ju-chat 工程需修改的 Output 类清单（8+ 个类）。
- [x] 已明确 `phone` 字段赋值来源改为 `phoneMask` 或 `phoneMaskForDisplay()`。
- [x] 已明确 `phone_mask` 为 NULL 时通过 `phone_aes` 现算掩码或返回 null。
- [x] 已明确导出功能中 `phone` 字段同样返回掩码值。
- [x] 已明确后端间接口不受 `phone` 字段返回掩码的影响。

## 专项检查：保存掩码格式校验

- [x] 已明确掩码格式检测规则：包含 `****`（连续 4 个星号）。
- [x] 已明确掩码格式校验在 `isWritablePhoneInput()` 之前执行。
- [x] 已明确掩码格式错误提示为 `手机号为掩码格式，请输入明文手机号`。
- [x] 已明确 MD5 格式错误提示保持为 `手机号加密格式不符`。
- [x] 已明确掩码格式校验不影响查询接口。
- [x] 已明确 AES 密文不会触发掩码格式误判。

## 专项检查：受影响的接口

- [x] 已按模块列出 drh 工程受影响的接口（drh-kk-cms 17+、drh-pay 1+、drh-endpoint 3+、drh-callback 3+、drh-media-process 2+）。
- [x] 已按模块列出 ju-chat 工程受影响的接口（ai 5+）。
- [x] 已区分查询接口（返回改造）和保存接口（校验增加）两种影响类型。

## 备注

- 本轮处于文档阶段，不涉及代码修改。
- 实现前需重新执行全量搜索，确认 Output/DTO 和保存入口清单无遗漏。
- 前置需求 032（DDL）、036（保存/查询改造）、041（XML phone_md5）的实现状态需确认。
