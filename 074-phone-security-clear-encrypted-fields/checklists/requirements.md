# 规格质量检查清单：生产手机号安全字段清空

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-11`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标数据库 profile、目标清单来源和执行脚本位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确禁止改变源手机号列、DDL、业务代码和回填接口。
- [x] 明确候选核对只按同组 `*_md5` 非空，不按 `mask/aes` 单独有值判断。
- [x] 明确低锁策略：主键小批量、每批提交、锁等待超时和重试。
- [x] 明确后续必须增加静态验证、SQL 分析、dry-run 和 verify 记录。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖正常路径、锁等待边界和不回归路径。
- [x] 边界情况已识别，并明确跳过、中止、重试或记录报告的策略。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机。
- [x] 已列出下游读取字段清单。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 或占位参数。
- [x] 不涉及业务下游调用后赋值风险。
- [x] 数据库写入关键参数已有静态验证和 dry-run 方案。
- [x] 生产写入需要人工确认备份和暂停写入任务。

## 实施就绪度

- [x] 实现范围限定在 074 规格目录。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ/Redis/配置契约。
- [x] 已确认旧逻辑中必须保持不变的字段、接口和 DDL。
- [x] 每个关键需求至少有一条测试、dry-run 或静态验证任务。
- [x] 验证计划避免导出或打印真实手机号安全字段值。
- [x] 如后续执行生产写入，必须同步更新 `tasks.md` 执行记录。

## 备注

- 生产 execute 前必须确认 RDS 备份/PITR 和写入任务暂停。
- 真实执行报告只能记录表名、字段组、候选量、更新量、批次数和错误摘要，不得记录字段值。
