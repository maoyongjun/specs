# 规格质量检查清单：JSONL 日志 file_url 解析与视频下载

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-05`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、源文件、输出目录和建议脚本位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增行为：解析 JSONL、提取 `file_url`、去重、下载、生成清单。
- [x] 明确禁止行为：不修改源 JSONL、不默认覆盖已有非空文件、不输出完整 prompt、不访问业务服务。
- [x] 明确异常处理：解析失败、非法 URL、HTTP 失败、写文件失败不中断全局任务。
- [x] 明确后续实现必须增加测试或静态验证记录。

## 需求完整性

- [x] 无未替换占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量：当前源文件 dry-run 应为 `260` 条行级 URL、`215` 条唯一 URL。
- [x] 验收场景覆盖正常路径、重复 URL、已有文件、坏 JSON、缺字段、HTTP 失败和复跑。
- [x] 边界情况已识别，并明确跳过、兜底、失败记录或退出策略。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机。
- [x] 已列出下游读取字段清单。
- [x] 没有未解释的空 JSON、空 Map 或占位下载任务。
- [x] 下游读取字段在下载前必须已解析和校验。
- [x] 不存在未处理的调用后赋值风险。
- [x] 外部 HTTP 下载的关键参数已有测试断言方案。
- [x] 本规格不改变调用顺序、接口契约、远程业务调用或业务语义。

## 实施就绪度

- [x] 实现范围已限定为离线脚本和测试，不扩散到无关模块。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ/Redis/配置契约。
- [x] 已确认必须保持不变的旧行为：源文件不变、已有非空文件默认不覆盖。
- [x] 每个关键需求至少有一条测试、dry-run 或静态验证任务。
- [x] 单元测试计划避免真实访问外部 HTTP，真实下载需由用户确认后单独执行。
- [x] 补充需求或纠正需求时，需要同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`。

## 备注

- 当前已完成文档创建、源文件静态解析确认、临时脚本创建和真实下载。
- 下载结果：`215` 个唯一 URL 全部下载成功，目标目录无零字节文件、无 `.part` 残留。
