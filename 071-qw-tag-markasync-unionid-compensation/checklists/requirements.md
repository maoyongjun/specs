# 规格质量检查清单：QW Tag MarkAsync UnionId 查询补偿打标

**用途**：验证需求完整性、参数完整性和生产执行门禁  
**创建日期**：`2026-06-10`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增、修改和禁止改变的行为。
- [x] 明确日志、幂等、异步 MQ、三次 get、生产副作用和异常处理要求。
- [x] 明确后续真实执行必须增加 SQL 分析、查询结果、HTTP 返回和任务表验证记录。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖正常路径、边界路径和不回归路径。
- [x] 边界情况已识别，并明确跳过、停止确认、记录日志或继续验证的策略。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机。
- [x] 已列出下游读取字段清单。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 或占位参数。
- [x] 下游读取字段在调用前已赋值，或在当前层现算现用。
- [x] 不存在未处理的调用后赋值风险。
- [x] 外部 HTTP、MQ、FC、OTS 和数据库查询的关键参数已有验证方案。
- [x] 已记录数据库 `external_userid` 与 HTTP `external_user_id` 的字段差异。
- [x] 已记录 `union_id` 为空或不唯一时不得调用生产接口。

## 生产执行门禁

- [x] 已创建实际 SQL 文件并替换真实 `external_userid`。
- [x] 已执行 `db_skill.py analyze` 并确认 SQL 为 `readonly`。
- [x] 已使用 `prod-mysql` profile 查询到唯一有效 `union_id`。
- [x] 已复核 `source/status` 与本次补偿意图一致。
- [x] 已获得用户对生产 HTTP 调用的明确确认。
- [x] 已记录 `markAsync` 返回的 `taskId/status`。
- [x] 已查询 `drh_qw_external_tag_task` 和 `drh_qw_external_tag_task_log` 验证结果。

## 实施就绪度

- [x] 实现范围已限定，不扩散到无关模块。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ/Redis/配置契约。
- [x] 已确认旧逻辑中必须保持不变的参数校验、幂等、MQ、限流、企微代理和 get 确认链路。
- [x] 每个关键需求至少有一条测试、静态验证或后续执行验证任务。
- [x] 文档创建阶段未访问生产数据库、RocketMQ、FC、OTS 或外部 HTTP；D002 阶段已按用户确认执行生产只读查询和生产 HTTP 调用。
- [x] 补充需求或纠正需求时，已要求同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`。

## 备注

- 当前阶段只完成 Spec Kit 文档创建，生产执行门禁保持未勾选。
- 后续若用户要求实际查询或调用生产接口，必须先补 D002 执行记录，再按门禁逐项推进。
