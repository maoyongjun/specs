# 规格质量检查清单：钢琴用户级人工回复静默（AI/SOP）

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-05-20`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [ ] 目标目录、模块、入口和最终发送点已明确。
- [ ] 用户目标、成功标准和非目标已明确。
- [ ] 新增、修改和禁止改变的行为已明确。
- [ ] `sop-reply` 不再额外写 `skuId == 4` 分支这一点已明确。

## 需求完整性

- [ ] 无占位内容残留。
- [ ] 需求可测试且无明显歧义。
- [ ] 成功标准可衡量。
- [ ] 验收场景覆盖正常路径、边界路径和不回归路径。
- [ ] 边界情况已识别，并明确跳过、兜底、抛错或记录日志的策略。

## 参数完整性门禁

- [ ] 已列出 `skuId` 的来源和赋值时机。
- [ ] 已列出 `ai-reply` 和 `sop-reply` 的下游读取点。
- [ ] 没有未解释的空 DTO、空 JSON、空 Map 或占位参数。
- [ ] 入口检查和最终发送前检查都已写入规格。
- [ ] 不存在未处理的调用后赋值风险。

## 实施就绪度

- [ ] 实现范围已限定，不扩散到无关模块。
- [ ] 不修改 `delay-mq`，除非规格明确要求。
- [ ] 已确认旧逻辑中必须保持不变的过滤、异常、日志、延迟和 fallback。
- [ ] 每个关键需求至少有一条测试或静态验证任务。

