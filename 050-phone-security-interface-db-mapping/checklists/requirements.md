# 规格质量检查清单：手机号安全改造——接口影响与数据库表全量映射

**用途**：验证映射文档完整性和测试就绪度  
**创建日期**：`2026-06-04`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目（drh、ju-chat）和模块（drh-pay、drh-endpoint、drh-kk-cms、drh-callback、drh-media-process）。
- [x] 明确用户目标（全量接口 × 数据库表映射）、成功标准（SC-001~SC-004）和非目标（不直接执行 DDL/回填）。
- [x] 明确每个接口的改动类型（保存/查询/展示/回调）。
- [x] 明确数据库表读写方向（W/R/D/—）。
- [x] 明确前端页面与接口的映射关系。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留。
- [x] 需求可验证且无明显歧义。
- [x] 成功标准可衡量（4 项标准）。
- [x] 覆盖全部 5 个微服务模块。
- [x] 覆盖全部 28 张已改造数据库表。

## 映射完整性门禁

- [x] 每个接口标注了改动类型（保存/查询/展示/回调）。
- [x] 每个接口标注了涉及的数据库表。
- [x] 每个接口标注了前端调用页面（或标注无前端页面）。
- [x] 数据库表 × 接口读写矩阵覆盖 28 张表 × 5 个模块。
- [x] 前端页面清单覆盖投放页面、CMS 后台页面、小程序/APP 页面和中转页面。
- [x] P2 表（`drh_xe_order` 等）已在文档中标注为"暂不写安全字段"。

## 测试就绪度

- [ ] 测试库已执行 032、048 和 D003 补充 DDL，28 张表具备安全字段和索引。
- [ ] 历史数据回填已执行，目标表记录安全字段有值。
- [ ] 前置规格（032/036/041/048）代码改造已合入测试分支。
- [ ] 测试数据已准备（含安全字段和 phone 为空的记录）。
- [ ] tasks.md 中 T011~T040 验证任务已分配到测试人员。

## 备注

- 本文档用于测试验证，不用于代码实现指导。
- 如有新增投放页面或接口，需补充到 spec.md 对应章节。
- P2/P3 表的后续改造需要单独创建新的 spec-kit。
