# 规格质量检查清单：AI 用户画像聚合查询接口

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-15`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置（kkhc-idc/ai + coze external-info-select）。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增、修改和禁止改变的行为（新增端点/服务；不改非私域逻辑与签收定时处理）。
- [x] 明确日志、缓存、fallback、异常处理要求（子对象查不到给默认值；私域异常降级）。
- [x] 明确后续实现必须增加测试或静态验证记录。

## 需求完整性

- [x] 无未替换占位内容残留（spec 正文）。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖正常路径、边界路径和不回归路径。
- [x] 边界情况已识别，并明确跳过/兜底/抛错/记录日志策略。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机（unionId/externalUserId/phone/campId/groupId/goodsId/kapi 参数）。
- [x] 已列出下游读取字段清单（CollectOrder/体验课/正价课/物流/kapi/OSS）。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 或占位参数。
- [x] 下游读取字段在调用前已赋值或现算（unionId 先于子查询、campId 先于 courseLink、phone 先于物流）。
- [x] 不存在未处理的调用后赋值风险。
- [x] 外部接口/HTTP/OSS/Redis 关键参数已有下游断言方案（见 tasks T011/T014）。
- [x] 改变调用顺序/契约/远程调用/语义的点已记录并完成用户确认（新增端点 + 缓存 + coze 调用 + 4 项设计选择）。

## 实施就绪度

- [x] 实现范围已限定（新增端点/服务/DTO/mapper/配置 + coze 私域分支一处调用）。
- [x] 不新增数据库表；新增对外 HTTP 端点与 Redis 缓存 key 已在规格明确。
- [x] 已确认旧逻辑必须保持不变项（AiServiceImpl 实时物流/定时处理、coze 非私域、setTushu）。
- [x] 每个关键需求至少有一条测试/编译/静态验证任务（tasks Phase 4）。
- [x] 单元测试计划 mock Redis/OTS/HTTP/OSS/ShowAPI，不真实联调（除端到端核对）。
- [x] 补充需求（私域 + externalUserId）已同步更新 spec/tasks/AGENTS。

## 备注

- 强制门禁未完成前，不进入实现：编码前必须完成 tasks 的 V1-V7 待核实项。
- 发现历史代码风险时，先补文档和测试映射，再改代码。
