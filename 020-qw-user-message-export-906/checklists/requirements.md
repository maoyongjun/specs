# 规格质量检查清单：906 私聊聊天记录导出

**用途**：在进入实现前验证规格完整性和质量  
**创建日期**：2026-05-18  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 规格聚焦业务可见行为：三账号私聊聊天记录导出。
- [x] 明确记录只导出 `yangfan`、`LiYan`、`ZengYan`。
- [x] 明确记录仅导出最近 10 天的聊天记录。
- [x] 明确记录老师和用户双方消息都要导出。
- [x] 明确记录不做 `isSelf` 过滤。
- [x] 明确记录排除群聊消息。
- [x] 明确记录 `chat_name` 以 `906` 开头。
- [x] 明确记录输出字段为 `chat_name`、`contact_name`、`text`。
- [x] 明确记录输出列顺序固定且使用 `TAB` 分隔。
- [x] 面向产品、测试和开发均可读。
- [x] 所有必填章节已完成。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 标记残留。
- [x] 需求可测试且无歧义。
- [x] 成功标准可衡量。
- [x] 成功标准与业务结果绑定。
- [x] 所有验收场景已定义。
- [x] 边界情况已识别。
- [x] 范围边界清晰：Spec Kit 文档阶段只写文档；实现阶段代码范围限定在 `qw-user-message-export`。
- [x] 依赖和假设已识别。

## 功能就绪度

- [x] 所有功能需求都有清晰验收条件。
- [x] 用户场景覆盖三账号范围、最近 10 天、消息双方保留、群聊排除、`906` 前缀过滤和固定三列输出。
- [x] 功能满足成功标准中定义的可衡量结果。
- [x] 规格文档与任务清单职责分离。

## 备注

- 用户已确认：`yangfan`、`LiYan`、`ZengYan` 按 `qwUserId` 处理。
- 用户已确认：查询记录范围为最近 10 天。
- 用户已确认：`chat_name` 的“906开头”按前缀过滤理解。
- 用户已确认：输出格式采用纯文本行输出，列之间使用 `TAB` 分隔。
- 当前阶段只写 Spec Kit 文档，不修改业务代码。
