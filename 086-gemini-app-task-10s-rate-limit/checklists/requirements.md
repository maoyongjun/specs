# 规格质量检查清单：Gemini AppTask 10 秒限速与突发保护

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-12`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增、修改和禁止改变的行为。
- [x] 明确时间、延迟、异常处理和旧逻辑保持要求。
- [x] 明确测试和静态验证记录。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖正常路径、突发路径和不回归路径。
- [x] 边界情况已识别，并明确跳过、兜底或抛错策略。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机。
- [x] 已列出下游读取字段清单。
- [x] 没有未解释的空 DTO、空 JSON、空 Map 或占位参数。
- [x] 下游读取字段在调用前已赋值，或在当前层现算现用。
- [x] 不存在未处理的调用后赋值风险。
- [x] 外部 FC 延迟重投递参数已有测试和代码断言路径。
- [x] 共享限流工具不改动，业务语义变化限定在 AppTask 音频链路。

## 实施就绪度

- [x] 实现范围已限定，不扩散到无关模块。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ/Redis/配置契约；仅新增 AppTask 独立 Redis key。
- [x] 已确认旧逻辑中必须保持不变的异常、日志、延迟重试和 fallback。
- [x] 每个关键需求至少有一条测试、编译或静态验证任务。
- [x] 单元测试避免真实访问 Redis、FC 或外部 HTTP。
- [x] 补充需求已同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`。

## 备注

- 800 突发提交不会在函数内长时间等待，也不会提前持有音频 Base64。
- FC async delay 超过 3600 秒时通过内部预订时间分段唤醒。
