# 规格质量检查清单：从视频中提取单音旋律的音符序列

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-21`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置（`videoToAudio/note_extractor.py`、`cli.py`、`tests/`）。
- [x] 明确用户目标、成功标准和非目标（单音旋律、直接返回、不做 OSS/回调/复音转录/MIDI 渲染）。
- [x] 明确新增、修改和禁止改变的行为（新增 librosa 提取；保留 FFmpeg `-y`/`-vn`、latin-1 解码、唯一文件名、临时清理；禁止 OSS/回调）。
- [x] 明确日志、时间、幂等、fallback、兼容性或异常处理要求（中文日志、UTC+8 时间戳、FFmpeg 失败抛错、空结果不报错、Python 3.10 兼容）。
- [x] 明确后续实现必须增加测试或静态验证记录（FFmpeg 参数断言、合成音频音符断言、静音段断言）。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 残留；占位内容已全部替换。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量（SC-001~SC-005 含具体数值/条件）。
- [x] 验收场景覆盖正常路径（旋律视频）、边界路径（静音/无音高）、不回归路径（保留 FFmpeg 旧参数与容错）。
- [x] 边界情况已识别并明确策略（无音轨/不可达/全静音/毛刺/同音合并/ffmpeg 缺失）。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机（见 spec「历史问题防漏分析」）。
- [x] 已列出下游读取字段清单（FFmpeg 阶段、librosa 阶段、音符分段阶段）。
- [x] 没有未解释的占位参数（Python 项目，等价空结果风险已定策略）。
- [x] 下游读取字段在调用前已赋值，或在当前层现算现用（audio_path 严格在 FFmpeg 成功后才交给 librosa）。
- [x] 不存在未处理的调用后赋值风险（已用「严格顺序 + 文件存在性校验」处理）。
- [x] 外部调用关键参数已有断言方案（FFmpeg 命令、pyin 参数用单元测试断言）。
- [x] 是否改变调用顺序/接口/远程调用/业务语义已记录：相对参照源码移除 OSS/回调（用户已确认），无其他未确认变化。

## 实施就绪度

- [x] 实现范围限定在 `videoToAudio` 新目录，不扩散到无关模块。
- [x] 不新增数据库表/对外 API/MQ/Redis/配置契约（本项目根本不涉及）。
- [x] 已确认必须保持不变的参照源码行为（FFmpeg 参数、容错解码、唯一文件名、临时清理、日志）。
- [x] 每个关键需求至少有一条测试或验证任务（见 tasks Phase 4）。
- [x] 单元测试计划避免真实访问外部服务：用 mock subprocess + 程序内合成音频，不依赖真实视频/网络（FFmpeg 真跑仅在 FC 联调）。
- [x] 补充或纠正需求时，将同步更新 `spec.md`、`tasks.md`、`AGENTS.md` 与本清单（D003 已同步）。

## 备注

- 强制门禁已完成，等待用户确认进入实现。
- 入口为函数计算 `handler(event, context)`，直接 return 音符序列，不写本地文件、不 OSS、不回调。
- 本地不安装 librosa/ffmpeg：仅做 `py_compile` 语法检查 + `audio_ffmpeg.py` 命令断言测试；功能验证在函数计算（Python 3.10）由用户执行。代码保持 Python 3.10 兼容语法。
