# 规格质量检查清单：LiveCampGroup 手机号安全字段为空原因分析

**用途**：验证原因分析完整性、参数完整性和后续实施就绪度  
**创建日期**：`2026-06-09`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确当前不修改业务代码，仅创建分析规格文档。
- [x] 明确后续实现必须增加测试或静态验证记录。
- [x] 明确截图字段与 `GroupLiveBaseOutput` / `POST live/base/v3` 的匹配依据。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖普通用户 V3、V2 对照、特殊用户 V3 对照和空值边界。
- [x] 边界情况已识别，并明确哪些属于 DTO 漏赋值、哪些属于数据本身为空。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机。
- [x] 已列出下游读取字段清单。
- [x] 已解释 `new LiveUser()` 空对象 fallback 风险。
- [x] 已确认普通用户 V3 分支的下游读取字段 `phoneMask/phoneMd5/phoneAes` 当前未在返回前赋值。
- [x] 已确认后续流程不会自动补齐手机号安全字段。
- [x] 本阶段不涉及外部接口、Feign、FC、MQ、Redis 或数据库写入。
- [x] 已记录新要求：`phone` 返回 `phoneMask` 脱敏展示值。

## 实施就绪度

- [x] 实现范围已限定在 `LiveCampGroupServiceImpl#transRecordsToOutPuts`。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ/Redis/配置契约。
- [x] 已确认旧逻辑中必须保持不变的过滤、异常、日志、分页和 fallback。
- [x] 每个关键需求至少有一条测试、编译或静态验证任务。
- [x] 单元测试计划避免真实访问 Redis、OTS、Center、RocketMQ、FC 或外部 HTTP。
- [x] 补充需求或纠正需求时，需要同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`。

## 备注

- 当前结论是静态代码分析结论：普通用户 V3 分支 DTO 手动组装漏字段。
- 若数据库中对应 `drh_live_user.phone_*` 本身为空，应另开数据补偿或写入链路分析。
