# 规格质量检查清单：私域 AI Agent「请勿打扰」标签跳过回复

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-17`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目（`juzi-service`）、模块（`MessageServiceImpl` 私域分支）、入口（`handlePrivateDomainAiIfMatched`）和核心实现位置。
- [x] 明确用户目标（命中「请勿打扰」不回复并打印日志）、成功标准和非目标。
- [x] 明确新增（标签拦截）、修改（私域分支增加判断）和禁止改变的行为（非私域链路、契约）。
- [x] 明确日志（`private_domain_ai_do_not_disturb_skip` / warn 降级日志）、时间窗顺序、幂等无关、fallback（异常放行）、兼容性要求。
- [x] 明确后续实现必须增加测试或静态验证记录。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留（执行记录 D002/D003 为待填模板）。
- [x] 需求可测试且无明显歧义（数据源、范围、异常口径已确认）。
- [x] 成功标准可衡量（命中不发送、未命中照常、不回归、测试通过）。
- [x] 验收场景覆盖正常路径（命中/未命中）、边界路径（异常降级）和不回归路径（白名单未命中/自消息/时间窗外）。
- [x] 边界情况已识别，并明确放行、降级、记录日志策略。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机（`externalUserId`、`userId` 调用前赋值；标签现查现用）。
- [x] 已列出下游读取字段清单（`drh_external_user_info` 主键 `external_user_id`、列 `follow_user`、`FollowUser.Tag.tag_name`）。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 或占位参数。
- [x] 下游读取字段在调用前已赋值，或在当前层现算现用。
- [x] 不存在未处理的调用后赋值风险。
- [x] OTS 读取已有下游断言方案（mock/spy `selectExternalUserTags` 或 `hasDoNotDisturbTag`，断言是否下发回复）。
- [x] 本次不改变调用顺序契约、远程调用契约或业务语义；数据源与范围已用户确认。

## 实施就绪度

- [x] 实现范围限定在私域分支与私域常量，不扩散到无关模块。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ/Redis/配置契约。
- [x] 已确认旧逻辑中必须保持不变的过滤、异常、日志、延迟和 fallback。
- [x] 每个关键需求至少有一条测试或静态验证任务。
- [x] 单元测试计划避免真实访问 OTS（用纯函数 + spy/mock 隔离静态 OTS 调用）。
- [x] 补充/纠正需求时，已同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md` 的约定。

## 备注

- 强制门禁已完成，进入实现。
- 标签名「请勿打扰」按精确字符串匹配；若实际存在别名，按 D003 纠正。
