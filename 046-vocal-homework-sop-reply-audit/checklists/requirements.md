# 规格质量检查清单：声乐作业点评 SopReply 迁移核查

**用途**：验证核查文档完整性、参数完整性和后续实施就绪度  
**创建日期**：`2026-06-02`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增、修改和禁止改变的行为：本次仅新增文档，禁止改业务代码。
- [x] 明确日志、时间、延迟、幂等、fallback、兼容性或异常处理要求：文档记录当前 fallback、识别-only、群聊和异常分支必须保持。
- [x] 明确 `sopReply time window config is empty` 的日志原因、配置来源、直接影响和旧逻辑差异。
- [x] 明确现场排查的三步口径：作业点评开关、课程时间、调用链。
- [x] 明确后续实现必须增加测试或静态验证记录。

## 需求完整性

- [x] 无模板占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖正常路径、边界路径和不回归路径。
- [x] 边界情况已识别，并明确跳过、兜底、抛错或记录日志的策略。
- [x] 已区分时间窗口“配置为空”“当前时间不在窗口”“参数非法”“接口异常”四类日志/结果。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机。
- [x] 已列出下游读取字段清单。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 或占位参数；已记录旧逻辑中空 `HomeWorkResultDto` 的语义风险。
- [x] 下游读取字段在调用前已赋值，或在当前层现算现用。
- [x] 不存在本次文档创建导致的调用后赋值风险。
- [x] 外部接口、FC、Redis 或配置关键参数已有后续静态验证和运行时核验方案。
- [x] 已列出时间窗口相关 Redis key、Center 接口和 `live_end_time` 字段来源。
- [x] 已列出作业点评开关配置样例、课程时间 SQL 和 `Delay-mq -> sop-reply` 调用链。
- [x] 若后续修复会改变调用顺序、接口契约、远程调用或业务语义，已记录必须完成用户确认。

## 实施就绪度

- [x] 实现范围已限定：仅新增 `specs/046-vocal-homework-sop-reply-audit` 文档目录。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ、Redis、配置契约。
- [x] 已确认旧逻辑中必须保持不变的过滤、异常、日志、延迟和 fallback。
- [x] 每个关键需求至少有一条测试、编译或静态验证任务。
- [x] 单元测试计划避免真实访问 Redis、OTS、Center、RocketMQ、FC 或外部 HTTP；本次只做静态验证。
- [x] 补充需求或纠正需求时，需要同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`。

## 备注

- 本次清单针对核查文档，不代表线上运行时配置已确认。
- 要确认“线上是否完全不走 `homework-review`”，必须查询 `homeWorkSopReplyPercent` 和 FC 环境变量。
