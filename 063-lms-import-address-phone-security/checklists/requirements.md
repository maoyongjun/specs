# 规格质量检查清单：lms 批量导入地址手机号安全补充

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-09`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增、修改和禁止改变的行为。
- [x] 明确 DDL、历史回填、查询、展示和 ERP 明文读取口径。
- [x] 明确后续实现必须增加测试或静态验证记录。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或模板占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖正常路径、边界路径和不回归路径。
- [x] 边界情况已识别，并明确跳过、兜底、抛错或记录日志的策略。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机。
- [x] 已列出下游读取字段清单。
- [x] 没有未解释的空 DTO、空 JSON、空 Map 或占位参数。
- [x] 下游读取字段在调用前已赋值，或在当前层现算现用。
- [x] 不存在未处理的调用后赋值风险。
- [x] ERP 明文手机号读取已有兜底方案。

## 实施就绪度

- [x] 实现范围已限定，不扩散到无关接口。
- [x] 不修改 MQ、Redis key 或 HTTP 路径。
- [x] 已确认旧逻辑中必须保持不变的过滤、异常、日志、异步和 fallback。
- [x] 每个关键需求至少有一条测试、编译或静态验证任务。
- [x] 补充需求或纠正需求时，需同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`。

## 备注

- `drh_import_address_record_detail` 是本次对历史 `051` 的补充纠正。
- 目标环境必须执行 DDL 后再验证接口保存与查询。
