# 规格质量检查清单：AppTask day 数字入参兼容

**用途**：验证 `day` 数字入参兼容需求完整性和实现可测性  
**创建日期**：2026-05-09  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标代码文件。
- [x] 明确 `AppTask` 与 `DownTask` 均需要兼容。
- [x] 明确数字 `0` 到 `6` 的目标映射。
- [x] 明确现有 `d0` 到 `d6` 行为需要保持。
- [x] 明确非法值不做有效映射。
- [x] 所有必填章节已完成。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 标记残留。
- [x] 需求可测试且无歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖 `0`、`1` 到 `6`、原有 `dN` 输入和 `DownTask` 场景。
- [x] 边界情况已识别。

## 实施就绪度

- [x] 实现范围限定在 `AppTask`、`DownTask` 的 `day` 解析。
- [x] 不涉及 Redis、OTS、配置中心或外部接口改造。
- [x] 明确需要单元测试覆盖。
- [x] 测试命令已执行并记录结果。

## 备注

- 本需求不改变任务配置 key 的命名约定，只在服务端兼容调用方传入的数字 `day`。
