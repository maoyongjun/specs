# 规格质量检查清单：AppCollectOrderController 手机号安全返回补充

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-09`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增字段、app 特例返回 `phoneAes`、lms 返回掩码和禁止改变的行为。
- [x] 明确后续实现必须增加测试或静态验证记录。

## 需求完整性

- [x] 无模板占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖正常路径、空值边界和不回归路径。
- [x] 边界情况已识别，并明确兜底或保持原行为。

## 参数完整性门禁

- [x] 已列出 `phoneMask/phoneMd5/phoneAes/phone` 的来源和赋值时机。
- [x] 已明确 app 与 lms 的 `phone` 字段返回口径不同。
- [x] 已列出下游读取字段清单。
- [x] 没有未解释的空 DTO、空 JSON、空 Map 或占位参数。
- [x] 下游读取字段在返回前同步赋值。
- [x] 不存在调用后赋值或异步后补齐风险。

## 实施就绪度

- [x] 实现范围已限定在 `lms-common`、`app`、`lms` 的订单分页返回链路。
- [x] 不修改数据库、MQ、Redis、Feign 或 HTTP 路径。
- [x] 已确认旧逻辑中必须保持不变的分页、商品组装、物流单号、异常和 fallback。
- [x] 每个关键需求至少有一条测试、编译或静态验证任务。

## 备注

- 本规格是历史手机号安全整改的补充，不替代 `048`、`050`、`063` 等既有规格。
