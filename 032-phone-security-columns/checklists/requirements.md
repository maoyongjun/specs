# 规格质量检查清单：手机号安全字段扩展

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-05-26`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增字段、索引和禁止改变的旧字段。
- [x] 明确本阶段不涉及日志、时间、延迟、幂等、fallback 或异常处理变更。
- [x] 明确后续实现必须补充写入、查询、展示和回填验证。

## 需求完整性

- [x] 无澄清占位标记或未替换占位内容残留。
- [x] 需求可测试且字段口径清晰。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖展示、查询、单条解密三个路径。
- [x] 边界情况已识别，并明确执行前元数据检查和大表 DDL 风险。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机。
- [x] 已列出下游读取字段清单。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 或占位参数。
- [x] 下游读取字段在后续实现中需由当前层现算现用。
- [x] 不存在本阶段调用后赋值风险。
- [x] 本阶段无外部接口、Feign、FC、MQ、Redis 调用。
- [x] 数据库结构变更已明确为用户本次要求，并以 DDL 文档形式交付。

## 实施就绪度

- [x] 实现范围限定为用户指定的 6 张表。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ/Redis/配置契约。
- [x] 已确认旧逻辑中必须保持原始手机号字段不变。
- [x] 每个关键需求都有静态验证任务。
- [x] 单元测试计划不访问 Redis、OTS、Center、RocketMQ、FC 或外部 HTTP。
- [x] 补充需求或纠正需求时，需同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`。

## 备注

- 本阶段交付 DDL 草案，不代表已在任何环境执行。
- 后续回填前必须确认 AES 密钥管理、手机号归一化、MD5 大小写、批次策略和失败记录口径。
