# 任务清单：手机号安全字段扩展

**输入**：来自 `spec.md` 的功能规格  
**前置条件**：`spec.md`、`AGENTS.md`、`checklists/requirements.md`  
**测试**：本阶段为 DDL 文档交付，以静态核对为主；不连接数据库执行。

## Phase 1：代码事实确认

- [x] T001 复查用户需求和本目录 `AGENTS.md`，确认当前目标是数据库字段扩展文档和 DDL。
- [x] T002 用代码搜索确认目标表对应实体和已有手机号字段。
- [x] T003 确认 `drh_live_user` 需要覆盖 `phone` 与 `app_phone` 两个手机号字段。
- [x] T004 确认其余目标表的手机号字段为 `phone`。
- [x] T005 确认本阶段不涉及配置、Redis、MQ、Feign、FC、HTTP 或业务调用链变更。

**检查点**：T001-T005 已完成，进入 DDL 文档实现。

## Phase 2：风险门禁

- [x] T006 检查是否存在 DTO、空 JSON、空 Map 或占位参数传递风险：本阶段无业务代码变更。
- [x] T007 检查调用后赋值或异步补齐风险：本阶段无调用链变更。
- [x] T008 检查字段读取来源：后续列表读 `*_mask`，查询读 `*_md5`，单条解密读 `*_aes`。
- [x] T009 检查数据库影响：会新增字段和索引，但本阶段只产出脚本，不执行。
- [x] T010 记录需要后续确认的设计项：AES 密钥、加密模式、回填策略、是否清理明文字段。
- [x] T011 建立静态验证映射：6 张表、7 个手机号字段、21 个新增字段、7 个 MD5 索引。

**检查点**：风险已写入 `spec.md` 和 checklist。

## Phase 3：实现

- [x] T012 创建 `032-phone-security-columns` 规格目录。
- [x] T013 编写字段扩展 DDL 脚本。
- [x] T014 编写 `spec.md`，明确表、字段、索引、非目标和边界。
- [x] T015 编写 `AGENTS.md` 和 checklist，约束后续实现。

## Phase 4：测试与验证

- [x] T016 静态核对 DDL 覆盖 6 张表。
- [x] T017 静态核对 DDL 覆盖 7 个手机号字段。
- [x] T018 静态核对 DDL 包含 21 个新增字段。
- [x] T019 静态核对 DDL 包含 7 个 `*_md5` 索引。
- [x] T020 确认本阶段未执行数据库变更、未修改业务代码。

## 执行记录

### D001 - 文档记录

- 执行内容：创建手机号安全字段扩展规格文档。
- 验证方式：代码搜索确认目标实体和原字段。
- 自检结论：字段口径明确，后续密钥和回填策略仍需单独确认。

### D002 - 实现记录

- 实现内容：新增 `add-phone-security-columns.sql`，覆盖 6 张表、7 个手机号字段、21 个安全字段和 7 个查询索引。
- 测试命令：`rg -n "phone_mask|phone_md5|phone_aes|app_phone_mask|app_phone_md5|app_phone_aes" specs\032-phone-security-columns`
- 测试结果：静态验证通过。
- 自检结论：本次只交付 DDL 和文档，未执行数据库操作。

### D003 - 纠正记录模板

- 触发原因：待后续填写，例如用户补充、测试失败、代码审查发现或字段口径调整。
- 修正内容：待后续填写，需说明具体修正。
- 文档同步：待后续填写，需说明同步了哪些文件。
- 验证结果：待后续填写，需说明测试或静态验证结果。
