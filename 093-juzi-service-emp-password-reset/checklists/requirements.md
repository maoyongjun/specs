# 规格质量检查清单：员工密码重置管理页

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-16`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目（`data-RC/juzi-service`）、模块（配置中心后台管理页）、入口（页面 + `admin/emp-password-reset` 接口）和核心实现位置（`EmpPasswordResetService`）。
- [x] 明确用户目标（按 `id`+`user_name` 校验后重置两表密码）、成功标准（SC-001~005）和非目标（不改登录校验/不调 FC/不批量/不新增表）。
- [x] 明确新增（页面+两接口+服务+DTO+测试）、修改（`WebConfig`/`ConfigPageAuthInterceptor`/`index.html` 新增注册项）和禁止改变（既有页面/接口/鉴权/`PhoneSecurityBackfillService`）的行为。
- [x] 明确日志（不输出密码值，只记表名/列名/`id`/`userName`/行数）、幂等（固定值幂等）、异常（未命中报错、SQL 异常向上抛）、并发（`user_name` 条件）要求。
- [x] 明确后续实现必须增加单元测试与下游 SQL/参数断言。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留（执行记录 D002/D003 为待回填模板，属约定保留项）。
- [x] 需求可测试且无明显歧义（FR-001~010 均可验证）。
- [x] 成功标准可衡量（命中/未命中、更新行数、不回归、测试通过）。
- [x] 验收场景覆盖正常路径（命中重置）、边界路径（未命中、参数非法、`drh_kk_emp` 0 行/多行）、不回归路径（既有页面接口）。
- [x] 边界情况已识别，并明确跳过/报错/抛出/记录策略。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机（`id`/`userName` 请求体并入口校验；`password` 固定常量；表名/列名固定）。
- [x] 已列出下游读取字段清单（`validate`/`reset` 的 SELECT/UPDATE 字段）。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 或占位参数。
- [x] 下游读取字段在调用前已赋值（`password` 常量、`id`/`userName`/`emp_one_id` 入口确定）。
- [x] 不存在未处理的调用后赋值风险（`reset` 严格先校验后更新）。
- [x] 数据库写入的关键参数已有下游参数断言方案（UPDATE SQL 文本 + `password` 常量 + `id`/`userName`/`emp_one_id` 绑定）。
- [x] 修复会改变数据库写入与业务语义：已记录，且写入由用户原始需求授权。

## 实施就绪度

- [x] 实现范围已限定，不扩散到无关模块（仅新增页面/接口/服务/DTO + 三处注册改动）。
- [x] 不新增数据库表、不新增对外 API（仅后台鉴权下的运维接口）、不修改 MQ/Redis/配置契约。
- [x] 已确认旧逻辑中必须保持不变的页面、接口、鉴权、服务。
- [x] 每个关键需求至少有一条测试/编译/静态验证任务（见 tasks T019-T022）。
- [x] 单元测试计划避免真实访问 Redis/OTS/RocketMQ/FC/外部 HTTP（全部 mock `JdbcTemplate`）；真实写库仅在运维阶段并先备份。
- [x] 补充/纠正需求时，会同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`。

## 备注

- 强制门禁已完成，已进入实现。
- 写真实库前必须先用 `database-sql-skill` 只读核对列名并备份两表 `password`（参考 057 规格的备份/恢复记录）。
