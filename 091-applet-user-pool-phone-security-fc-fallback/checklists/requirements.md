# 规格质量检查清单：AppletUserPool 分页 phone 掩码补算

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-16`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目（`drh-kk-cms`）、模块（`AppletUserPoolServiceImpl.getPageList`）、入口和核心实现位置。
- [x] 明确用户目标（空掩码走函数计算补算）、成功标准和非目标。
- [x] 明确新增（空掩码兜底 + 三安全字段回填）、修改（phone 赋值分支）和禁止改变（路径/入参/分页/权限/渠道价格 key）的行为。
- [x] 明确兜底（失败返回 null）、不泄露明文、无格式校验等要求。
- [x] 明确后续实现以编译 + 静态验证作为测试映射。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留。
- [x] 需求可测试且无明显歧义（FR-001~FR-007）。
- [x] 成功标准可衡量（SC-001~SC-005）。
- [x] 验收场景覆盖正常路径（掩码非空）、边界路径（空掩码补算/非标准号）、不回归路径（失败返回 null、不泄露明文）。
- [x] 边界情况已识别并明确策略（空 phone、函数计算失败、非标准号、性能边界）。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机：`phone/phoneMask/phoneMd5/phoneAes` 来自 `selectPoolPage`；空掩码时由 `buildPhoneSecurity` 现算。
- [x] 已列出下游读取字段清单：前端 `phone`（掩码展示）、`phoneAes`（受控解密）、`phoneMd5`（等值匹配）。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 或占位参数；函数计算失败按 `null` 兜底。
- [x] 下游读取字段在调用前已赋值或当前层现算：补算分支一次性赋值 mask/md5/aes/phone。
- [x] 不存在未处理的调用后赋值风险：phone 覆盖仍在 forEach 末尾，晚于渠道价格 key 读取。
- [x] 函数计算（FC）关键入参（原始 `phone`）已有静态断言方案；不联调真实函数计算。
- [x] 改变会引入额外远程调用（函数计算）与失败兜底口径，已记录并完成用户确认（spec D003）。

## 实施就绪度

- [x] 实现范围限定在 `getPageList` 的 phone 赋值一行，不扩散到其他方法/模块。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ/Redis/配置契约、不改 SQL。
- [x] 已确认必须不变的权限过滤、分页、渠道价格 key、其他字段装配与异常处理。
- [x] 每个关键需求有编译或静态验证任务对应（见 tasks.md Phase 4）。
- [x] 单元测试计划避免真实访问函数计算/OTS/Redis/DB；以编译 + 静态验证替代端到端联调。
- [x] 纠正/确认需求时已同步 `spec.md`、`tasks.md`、`AGENTS.md`、本清单。

## 备注

- 强制门禁已完成后才进入实现。
- 函数计算逐条调用的性能影响已作为剩余风险记录（tasks.md D002），假设绝大多数行已回填 `phone_mask`。
