# 规格质量检查清单：雅琪钢琴作业点评配置同步与绑定

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-23`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增、修改和禁止改变的行为。
- [x] 明确数据库写入门禁和接口调用要求。
- [x] 明确后续验证记录。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖三表同步、李瑶归属、雅琪命中和非目标保护。
- [x] 边界情况已识别。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机。
- [x] 已列出下游读取字段清单。
- [x] 已识别 `_Vxx.txt` 占位文件风险。
- [x] 下游读取字段在调用前有确定来源。
- [x] 不存在未处理的调用后赋值风险。
- [x] 数据库写入和接口创建已有验证方案。
- [x] 用户已确认执行计划。

## 实施就绪度

- [x] 实现范围限定在作业点评三表和测试接口。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ/Redis/配置契约。
- [x] 已确认旧钢琴 strategy/action 保持不变。
- [x] 每个关键需求至少有一条静态或接口验证任务。
- [x] 运行时匹配验证使用本地兼容脚本，不真实发送微信消息。

## 备注

- 写库 SQL 必须逐个分析。
- 生产库只读，测试库写入使用 `dev-mysql --allow-write --confirm`。
