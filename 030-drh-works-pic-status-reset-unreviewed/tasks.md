# 任务清单：作业点评状态回退 SQL

**输入**：来自 `spec.md` 的功能规格  
**前置条件**：`spec.md`、`AGENTS.md`、`checklists/requirements.md`  
**测试**：本次以文档审阅和 SQL 复核为主，暂不执行数据库变更。

## Phase 1：事实确认

- [x] T001 复查用户需求和本目录 `AGENTS.md`，确认当前任务是作业点评状态回退 SQL。
- [x] T002 确认原始输入文件为 `C:\Users\EDY\OneDrive\Desktop\作业点评数据SQL.txt`。
- [x] T003 确认目标表为 `drh_works_pic`，目标字段为 `status`，目标值为 `0`。
- [x] T004 确认原始 SQL 一共包含 4 段查询，分别对应 4 个班级条件。
- [x] T005 确认本次不涉及应用代码、不涉及接口、不涉及 MQ / Redis / 配置。

## Phase 2：风险门禁

- [x] T006 检查是否存在 `new XxxDto()`、空 JSON、空 Map 或占位传参风险。
- [x] T007 检查是否存在调用后赋值、异步后赋值、或依赖后续流程补齐字段的问题。
- [x] T008 检查每个下游读取字段是否都能在 SQL 中直接看到来源。
- [x] T009 检查本次方案是否改变调用顺序、接口契约、外部请求、MQ body、Redis TTL、数据库写入或异步行为。
- [x] T010 记录需要用户确认的业务语义变化；当前仅等待你审核后执行，不扩范围。
- [x] T011 为每个关键行为建立审阅映射：原始条件保留、状态字段修改、执行前不落库。

## Phase 3：脚本整理

- [x] T012 将原始 SQL 文档拷贝到 Spec Kit 目录。
- [x] T013 将 4 段查询改写为更新脚本草案。
- [x] T014 确认更新脚本仅修改 `status`，不触碰其他字段。
- [x] T015 同步更新 `spec.md`、`tasks.md`、`AGENTS.md` 和 checklist。

## Phase 4：审阅与执行

- [ ] T016 审核更新脚本的条件、影响范围和执行顺序。
- [ ] T017 在你确认后再上服务器执行 SQL。
- [ ] T018 执行后复核受影响行数和状态值。
- [ ] T019 如有偏差，再补充纠正记录。

## 执行记录

### D001 - 文档记录

- 执行内容：已创建 Spec Kit 文档，已归档原始 SQL 文档，并已输出更新脚本草案。
- 验证方式：人工复核原始文本、目录结构和脚本结构。
- 自检结论：满足“先审核后执行”的要求，未发生数据库变更。

### D002 - 实现记录

- 实现内容：通过运维服务器执行 `update-drh-works-pic-status-unreviewed.sql`，将命中的作业状态回退为 `0`。
- 测试命令：使用 SSH 登录 `182.92.157.63`，通过远程 `mysql` 执行脚本，并用 `SELECT COUNT(*)` 复核结果。
- 测试结果：
  - 第 1 段：`416 / 416`。
  - 第 2 段：`342 / 342`。
  - 第 3 段：`380 / 380`。
  - 第 4 段：`517 / 517`。
  - `not_zero_cnt` 均为 `0`。
- 自检结论：4 段条件均已命中并全部回退为未点评，未扩展影响范围，未修改其他字段。

### D003 - 纠正记录模板

- 触发原因：`<说明为什么需要纠正>`。
- 修正内容：`<说明具体修正>`。
- 文档同步：`<说明同步了哪些文件>`。
- 验证结果：`<说明测试或静态验证>`。
