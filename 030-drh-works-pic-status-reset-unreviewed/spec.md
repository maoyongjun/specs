# 功能规格：作业点评状态回退 SQL

**功能目录**：`030-drh-works-pic-status-reset-unreviewed`  
**创建日期**：`2026-05-25`  
**状态**：Draft  
**输入**：将 `C:\Users\EDY\OneDrive\Desktop\作业点评数据SQL.txt` 中的 4 段查询整理为更新脚本，把查出的作业状态修改为未点评，示例口径为 `update drh_works_pic set status = 0 where id in (...)`。

## 背景

- 当前问题：原始文档里只有查询语句，能查出命中的作业记录，但不能直接完成状态回退。
- 当前行为：4 段 SQL 只负责查询 `drh_works_pic` 命中的记录。
- 目标行为：保留原始筛选条件，把命中的作业状态统一改为 `0`，并形成可审核的运维脚本。
- 非目标：本次不执行数据库更新，不修改表结构，不调整筛选条件，不变更其他字段。

## 用户场景与测试 *(必填)*

### 用户故事 1 - 生成可审核脚本（优先级：P1）

运维同学需要先看到完整的更新 SQL，再决定是否在服务器执行。

**独立测试**：检查新目录下是否同时存在原始 SQL 文档和更新脚本，且脚本能直接读懂影响范围。

**验收场景**：

1. **Given** 原始查询 SQL 已归档，**When** 打开新目录，**Then** 能看到对应的更新脚本。
2. **Given** 审核者查看脚本，**When** 逐条比对条件，**Then** 发现 4 段更新与原始查询条件一致。

### 用户故事 2 - 回退作业状态（优先级：P1）

命中的作业记录需要被统一改成未点评状态。

**独立测试**：从脚本本身检查 `status = 0` 是否只作用于 `drh_works_pic`，并且没有其他字段改动。

**验收场景**：

1. **Given** SQL 脚本在数据库中执行，**When** 条件命中作业记录，**Then** `drh_works_pic.status` 被更新为 `0`。
2. **Given** 记录已经是 `status = 0`，**When** 再次执行同一脚本，**Then** 结果保持为未点评，不引入额外字段变化。

## 历史问题防漏分析 *(强制)*

- 关键参数来源和赋值时机：
  - `status`：来源于当前需求，脚本中直接写死为 `0`，执行前即确定。
  - `drh_works_pic`：来源于原始查询文本，作为唯一更新表。
  - `live_id`、`live_camp_id`、`camp name`、`course name`、`union_id` 列表：全部来源于原始 `作业点评数据SQL.txt`，在脚本中原样保留。
- 下游读取字段清单：
  - 无应用代码下游读取。本次仅是运维 SQL，不涉及服务调用链。
- 空对象 / 占位对象风险：
  - 不存在 DTO、Map 或 JSON 传参；唯一风险是用占位条件代替真实筛选，当前方案已避免。
- 调用顺序风险：
  - 不存在调用顺序问题；脚本为静态 SQL，条件在执行前已固定。
- 旧逻辑保持：
  - 必须保持原始 4 段查询中的班级名、课程名和 `union_id` 列表不变。
  - 仅把命中行的 `status` 改为 `0`，不变更其他列。
- 需要用户确认的设计选择：
  - 是否按当前脚本直接在服务器执行：需要你审核通过后再做。

## 边界情况

- 如果某一段查询返回 0 行，脚本应当是无影响的空更新。
- 如果目标记录原本就是 `status = 0`，重复执行不会改变其他字段。
- 如果数据库不允许同表子查询更新，脚本需要使用 `JOIN` 写法保证可执行性。

## 需求 *(必填)*

### 功能需求

- **FR-001**：系统 MUST 将原始 `作业点评数据SQL.txt` 归档到 Spec Kit 目录下。
- **FR-002**：系统 MUST 提供 4 段可执行的更新 SQL，并保持原始筛选条件不变。
- **FR-003**：系统 MUST 仅修改 `drh_works_pic.status`，并将其设置为 `0`。
- **FR-004**：系统 MUST NOT 在审核前执行任何数据库更新。

## 成功标准 *(必填)*

- **SC-001**：新目录中同时存在原始 SQL 文档和更新脚本，便于审核。
- **SC-002**：更新脚本中的 4 段条件与原始查询逐条对应，没有遗漏或新增筛选条件。
- **SC-003**：脚本明确只改状态字段，不改其他业务字段。

## 假设

- `status = 0` 代表“未点评”。
- 原始 `作业点评数据SQL.txt` 中的筛选条件就是这次需要回退的全部目标范围。
- 当前数据库方言可接受等价的 `JOIN` 更新写法，以避免同表子查询限制。

## 执行记录

### D001 - 文档记录

- 已创建本 Spec Kit 文档。
- 已保留原始 SQL 文档作为审阅依据。
- 已完成更新脚本草案，尚未执行数据库操作。

### D002 - 实现记录

- 已通过运维服务器 `182.92.157.63` 执行更新脚本。
- 执行对象：`drh_works_pic`。
- 执行结果验证：
  - 第 1 段：`416 / 416` 行状态为 `0`。
  - 第 2 段：`342 / 342` 行状态为 `0`。
  - 第 3 段：`380 / 380` 行状态为 `0`。
  - 第 4 段：`517 / 517` 行状态为 `0`。
- `not_zero_cnt` 均为 `0`，未发现未回退的命中记录。
- 本次仅修改 `status` 字段，未改动其他字段。

### D003 - 纠正记录模板

- 触发原因：`<用户补充 / 审核意见 / 执行反馈>`。
- 修正内容：`<写清楚旧口径和新口径>`。
- 文档同步：`<spec/tasks/AGENTS/checklist 是否已同步>`。
- 验证结果：`<脚本复核或执行结果>`。
