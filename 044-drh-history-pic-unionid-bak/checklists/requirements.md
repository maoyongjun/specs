# 规格质量检查清单：drh_history_pic union_id 备份 SQL

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-01`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增、修改和禁止改变的行为。
- [x] 明确日志、时间、延迟、幂等、fallback、兼容性或异常处理要求。
- [x] 明确后续执行前必须先审核 SQL 脚本。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留。
- [x] 需求可审阅且无明显歧义。
- [x] 成功标准可检查。
- [x] 验收场景覆盖正常路径、边界路径和不回归路径。
- [x] 边界情况已识别，并明确无影响、跳过、默认回滚或需要人工提交的策略。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机。
- [x] 已列出下游读取字段清单。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 或占位参数。
- [x] 下游读取字段在脚本中已直接体现，或无应用下游调用变更。
- [x] 不存在未处理的调用后赋值风险。
- [x] 数据库更新的关键参数已有审阅和复核方案。
- [x] 若修复会改变业务语义，已记录并在用户确认后执行。

## 实施就绪度

- [x] 实现范围已限定，不扩散到无关模块。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ / Redis / 配置契约。
- [x] 已确认旧逻辑中必须保持不变的过滤条件和目标字段。
- [x] 每个关键需求至少有一条审阅、执行或静态验证任务。
- [x] 未要求联调外部系统。
- [x] 补充需求或纠正需求时，会同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`。

## SQL 审阅清单

- [x] 初始 SQL 目标 `union_id` 列表包含 5 个值，第二批 SQL 目标 `union_id` 列表包含新增 92 个值。
- [x] 课程名固定为 `胡琴说（上）`。
- [x] 临时表字段包含 `id`、`pic_id`、`source_union_id`、`target_union_id`、`live_name`。
- [x] `UPDATE` 只设置 `hp.union_id = t.target_union_id`。
- [x] 更新后提供 `target_count`、`updated_count`、`not_updated_count` 复核。
- [x] 已执行提交后复核，目标原始 `union_id` 历史点评记录不再命中。
- [x] 更新后提供 `/works/songScore` 运维接口参数输出。
- [x] 运维接口参数文件已按只读查询结果生成。
- [x] SQL 默认以 `ROLLBACK` 收尾。
- [x] 第二批提交后已独立复核：原始历史点评记录 0 条，`_bak` 历史点评记录 92 条。

## 备注

- 强制门禁已完成，并已按用户确认执行初始批次和第二批数据库更新。
- 这次需求的核心是“按示例条件圈定人工点评历史记录，只备份目标记录的 `union_id`”；第二批额外限定 `class_id=1124820`，避免误处理其他课程。
