# 规格质量检查清单：ai-reply 函数计算异步请求积压根因分析与优化建议

**用途**：验证分析完整性、结论可复算性与（建议落地时的）参数完整性
**创建日期**：`2026-06-17`
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目（fc/ai-reply）、模块、异步入口（AppTask）和核心实现（CozeUtil/RateLimitUtil）。
- [x] 明确分析目标、成功标准（可复算）与非目标（本阶段不改代码、不改 AI 语义）。
- [x] 明确区分根因（主因/放大因素）与必须保持不变的旧行为。
- [x] 明确日志/指标口径（durationMs、concurrentRequests、dwellLatencyMs、hasFunctionError、轮询/限速计数）与时间窗口。
- [x] 明确每条优化建议是否需后续测试或静态验证（Phase 4 已预置）。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留（D002/D003 为待填模板，已标注）。
- [x] 分析结论可测试/可复算（由日志统计与 Little 定律推导）。
- [x] 成功标准可衡量（中位/平均/最大时长、处理中并发、完成速率）。
- [x] 验收场景覆盖削峰收敛（正常）、长尾/同会话高频（边界）、不回归（旧逻辑保持）。
- [x] 边界情况已识别：尖峰、长尾 195s、同会话串行、限速 5 分钟等待、Redis 连接 churn。

## 参数完整性门禁（面向优化建议落地）

- [x] 已列出关键参数来源与赋值时机（botId/conversationId/messageModels/QPS 计数/运行锁）。
- [x] 已列出下游读取字段清单（Coze 入参、juzi-api taskObj）。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 占位参数；并明确禁止建议引入空占位。
- [x] 下游读取字段在调用前已赋值，或当前层现算（conversationId 现算并回写）。
- [x] 不存在未处理的调用后赋值风险。
- [x] 已为外部接口/FC/Redis 的关键参数预置「下游参数断言」测试方向（Phase 4 T019-T021）。
- [x] 凡会改变调用顺序、远程调用、Redis key/TTL、QPS 口径、FC 并发/触发器配置或 AI 语义的建议，均标注「需确认」，未确认不实施。

## 实施就绪度

- [x] 分析范围限定在 ai-reply 异步链路，未扩散到无关模块。
- [x] 本阶段不新增表、不新增对外 API、不改 MQ/Redis/配置契约；任何此类变更均列为需确认的建议。
- [x] 已确认旧逻辑必须保持不变项（跳过分支、gap、撤回、人工静默、敏感词重试、isSelf 忽略等）。
- [x] 每条可落地建议都有对应的实现任务与测试/验证任务（Phase 3/4）。
- [x] 单测计划避免真实访问 Redis/OTS/Coze/句子互动/FC，使用 mock 并断言下游参数。
- [x] 后续补充/纠正需求时，同步更新 spec/tasks/AGENTS/checklist 并追加 Dxxx。

## 备注

- 强制门禁未完成、且涉及配置/远程调用/语义的建议未获确认前，不进入实现。
- 「积压数 / 处理中数」的控制台精确定义、异步重试与队列保留配置、Coze/句子互动下游容量，均需向运维/控制台核实后再推进 A 类建议。
