# 规格质量检查清单：delay-mq 按 speakerId 路由 Coze botId

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-03`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增、修改和禁止改变的行为。
- [x] 明确日志、异常、fallback、兼容性和旧逻辑保持要求。
- [x] 明确后续实现必须增加测试或静态验证记录。

## 需求完整性

- [x] 无未澄清标记或未替换占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖正常路径、边界路径和不回归路径。
- [x] 边界情况已识别，并明确跳过、兜底、抛错或记录日志的策略。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机。
- [x] 已列出下游读取字段清单。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 或占位参数。
- [x] 下游读取字段在调用前已赋值，或要求在当前层现算现用。
- [x] 不存在未处理的调用后赋值风险。
- [x] 外部接口、Feign、FC、MQ、Redis 或数据库写入的关键参数已有下游参数断言方案。
- [x] 若修复会改变调用顺序、接口契约、远程调用或业务语义，已记录并完成用户确认；本需求新增 Center 查询为用户明确要求。

## 实施就绪度

- [x] 实现范围已限定，不扩散到无关模块。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ/Redis/配置契约。
- [x] 已确认旧逻辑中必须保持不变的过滤、异常、日志、延迟和 fallback。
- [x] 每个关键需求至少有一条测试、编译或静态验证任务。
- [x] 单元测试计划避免真实访问 Redis、OTS、Center、RocketMQ、FC 或 Coze，除非规格明确要求联调。
- [x] 补充需求或纠正需求时，已要求同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`。

## 备注

- 本阶段仅修改规格文档，不编码。
- 后续实现必须重新确认 `fc/delay-mq` 工作区状态，避免覆盖用户已有未提交改动。
- 重点防漏点是区分 Coze 请求的 `botID` 与 `EmpExternalDto.user_bot_id`，两者不能混用。
