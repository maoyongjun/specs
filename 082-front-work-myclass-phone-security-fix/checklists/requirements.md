# 规格质量检查清单：FrontWork/FrontMyClass 手机号安全补遗

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-12`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置（`C:\workspace\drh` drh-common/drh-kk-cms，入口与实现行号见 AGENTS.md）。
- [x] 明确用户目标、成功标准和非目标（phone 掩码+三件套；非目标含 task 宽表、内部明文链路、SQL/DDL）。
- [x] 明确新增、修改和禁止改变的行为（FR-001~FR-008）。
- [x] 明确日志、时间、延迟、幂等、fallback、兼容性或异常处理要求（掩码兜底链：mask → aes 解密掩码 → 11 位明文本地掩码 → null，不抛异常）。
- [x] 明确后续实现必须增加测试或静态验证记录（编译+静态扫描，口径同 080）。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量（SC-001~SC-005）。
- [x] 验收场景覆盖正常路径、边界路径和不回归路径（故事 1/2 正常+边界，故事 3 不回归）。
- [x] 边界情况已识别，并明确跳过、兜底、抛错或记录日志的策略。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机（`AppletUser.phone*` → 组装时/controller 边界，见 spec.md 防漏分析）。
- [x] 已列出下游读取字段清单（前端展示、getAllInfo、SmsTrigger×2/Outbound、CSV 拷贝）。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 或占位参数。
- [x] 下游读取字段在调用前已赋值，或在当前层现算现用（掩码现算现用）。
- [x] 不存在未处理的调用后赋值风险。
- [x] 外部接口、Feign、FC、MQ、Redis 或数据库写入的关键参数已有下游参数断言方案（本次无外部调用变化；返回对象组装内容以静态扫描断言，口径同 080）。
- [x] 若修复会改变调用顺序、接口契约、远程调用或业务语义，已记录并完成用户确认（2026-06-12 确认：范围 A+B、controller 边界掩码、task 路径排除）。

## 实施就绪度

- [x] 实现范围已限定，不扩散到无关模块（仅 drh-common 1 个 DTO + drh-kk-cms 7 个文件）。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ/Redis/配置契约，除非规格明确要求。
- [x] 已确认旧逻辑中必须保持不变的过滤、异常、日志、延迟和 fallback（tasks.md T005 清单）。
- [x] 每个关键需求至少有一条测试、编译或静态验证任务（tasks.md T011 映射）。
- [x] 单元测试计划避免真实访问 Redis、OTS、Center、RocketMQ、FC 或外部 HTTP（本次为编译+静态验证，无真实外部访问）。
- [ ] 补充需求或纠正需求时，已同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`（持续项，发生 Dxxx 时勾选）。

## 备注

- 强制门禁未完成前，不进入实现。
- 发现模型未识别的历史代码风险时，先补文档和测试映射，再改代码。
- 已知缺口（不阻塞本规格）：task 策略 `orderPage` 走宽表 `drh_front_emp_class_order`，实体无安全三字段，仍返回明文；线上默认 realtime 策略；后续独立规格处理宽表加列+生成链路。
