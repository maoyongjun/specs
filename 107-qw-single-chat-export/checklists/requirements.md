# 规格质量检查清单：单账号 5 月 23 日至 6 月 23 日私聊导出

**用途**：验证需求完整性、参数完整性和实施就绪度  
**创建日期**：`2026-06-23`  
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 明确目标项目、模块、入口和核心实现位置。
- [x] 明确用户目标、成功标准和非目标。
- [x] 明确新增、修改和禁止改变的行为。
- [x] 明确日志、时间、分页、文件切分、兼容性和异常处理要求。
- [x] 明确实现必须增加测试或静态验证记录。

## 需求完整性

- [x] 无 `[NEEDS CLARIFICATION]` 或未替换占位内容残留。
- [x] 需求可测试且无明显歧义。
- [x] 成功标准可衡量。
- [x] 验收场景覆盖正常路径、边界路径和不回归路径。
- [x] 边界情况已识别，并明确跳过、兜底、抛错或记录日志的策略。
- [x] 已明确本模式口径与既有私聊导出模式一致：排除群聊。

## 参数完整性门禁

- [x] 已列出关键参数来源和赋值时机（单账号、开始时间、结束时间、`user_id` 字段、输出目录）。
- [x] 已列出下游读取字段清单。
- [x] 没有未解释的 `new XxxDto()`、空 JSON、空 Map 或占位参数。
- [x] 下游读取字段在调用前已赋值，或来自 OTS 当前查询结果。
- [x] 不存在未处理的调用后赋值风险（账号、时间窗口均为常量推导，调用前确定）。
- [x] OTS 查询关键参数已有下游参数断言方案（私聊过滤即排除群聊、timestamp 闭区间、timestamp ASC、`user_id` 字段、`EXPORT_COLUMNS`）。
- [x] 用户已确认会改变实现口径的选择：`user_id` 字段、时间窗口、只导出私聊、沿用七列 CSV。

## 实施就绪度

- [x] 实现范围已限定，不扩散到无关模块。
- [x] 不新增数据库表、不新增对外 API、不修改 MQ、Redis 或配置契约。
- [x] 已确认旧逻辑中必须保持不变的私聊过滤、异常、日志、分页、文件切分和 fallback。
- [x] 每个关键需求至少有一条测试、编译或静态验证任务。
- [x] 单元测试计划避免真实访问 OTS，使用 fake repository 或构造 SearchRequest 断言参数。
- [x] 补充需求或纠正需求时，需要同步更新 `spec.md`、`tasks.md` 和 `AGENTS.md`。

## 备注

- 本规格为文档 + 实现一体；实现落在 `C:\workspace\ju-chat\qw-user-message-export`。
- 当前阶段未真实访问 OTS；真实跑数需配置 `endpoint`、`accessKey`、`accessSecret`、`instance`。
- 本模式与 `export`/`jun3-chat-export`/`may15-chat-export` 的查询口径一致（私聊过滤排除群聊、CSV、union_id、分页、切片、错误日志相同），仅固定账号列表和时间窗口不同。
